import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  sectionContainer: {
    paddingTop: 16,
    paddingLeft: 16,
    paddingRight: 16,
    paddingBottom: 0,
  },
});

export default styles;
